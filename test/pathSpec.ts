/// <reference path='../typings/angularjs/angular.d.ts' />
/// <reference path='../typings/angularjs/angular-mocks.d.ts' />
/// <reference path='../typings/jasmine/jasmine.d.ts' />

import {PathFactory} from "../src/path/pathFactory"

xdescribe("", () => {
    describe("Path", () => {
        it("Nodes should contain state objects", () => {
            expect(false).toBeTruthy();
        });

        it(".splice should return a new Path with spliced nodes", () => {
            expect(false).toBeTruthy();
        });
    });

    describe("IParamPath", () => {
        it("should spread parameter values across the nodes", () => {
            expect(false).toBeTruthy();
        });
    });
});

xdescribe("PathFactory", () => {
    describe("inheritParams", () => {
        it("should inherit parameter values for a portion of a path", () => {
            expect(false).toBeTruthy();
        });
    });
});